<%= form_with(model: hop) do |form| %>
  <% if hop.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(hop.errors.count, "error") %> prohibited this hop from being saved:</h2>

      <ul>
        <% hop.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
